#include<stdio.h>
int main(){
    float length,breadth,area;
    printf("length: ");
    scanf("%f",&length);
    printf("breadth: ");
    scanf("%f",&breadth);
    area = length * breadth; // follow sequence
    printf("Area is: %f",area);
}